# Copy to .env and adjust if you need to point the suite elsewhere.
# Both have working defaults, so .env is optional.
WEB_BASE_URL=https://www.cheapflights.com.au
API_BASE_URL=https://restful-booker.herokuapp.com
API_USERNAME=admin
API_PASSWORD=password123
